generator client {
  provider      = "prisma-client-js"
  binaryTargets = ["windows"]
  engineType    = "binary"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model produto {
  id   Int    @id
  nome String @db.VarChar(100)
}
